FACTS: 
PERCEPTS: free(INT); assigned(INT) 
VARSINT: id; slot
VARSREAL:
DISCRETE: 
DURATIVE: park(INT); exiting_zone()
TIMERS: timer1
INIT: id:=9

<TR>
assigned(out slot) -> park(slot)
free(out slot) && timer1.end -> park(slot)
high_demand && !free(out slot) -> exiting_zone()
True -> _send("new_car", id); timer1.start(600)
